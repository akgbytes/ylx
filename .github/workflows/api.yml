name: API CI/CD

on:
  pull_request:
    paths:
      - "apps/api/**"
      - ".github/workflows/api.yml"

  push:
    branches:
      - main
    paths:
      - "apps/api/**"
      - ".github/workflows/api.yml"

  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: api-${{ github.ref }}
  cancel-in-progress: false

defaults:
  run:
    working-directory: apps/api

jobs:
  ci:
    name: Check, test, and build
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Check out the repository
        uses: actions/checkout@v7

      - name: Setup Go
        uses: actions/setup-go@v7
        with:
          go-version-file: apps/api/go.mod
          cache-dependency-path: apps/api/go.sum

      - name: Download dependencies
        run: go mod download

      - name: Verify dependencies
        run: go mod verify

      - name: Check module files
        run: go mod tidy -diff

      - name: Install golangci-lint
        uses: golangci/golangci-lint-action@v9
        with:
          version: v2.12.2
          working-directory: apps/api
          install-only: true

      - name: Check formatting
        run: golangci-lint fmt --diff

      - name: Run linters
        run: golangci-lint run ./...

      - name: Install govulncheck
        run: go install golang.org/x/vuln/cmd/govulncheck@v1.6.0

      - name: Run vulnerability check
        run: govulncheck ./...

      - name: Run tests with race detector and coverage
        run: |
          go test -race -coverprofile=coverage.out ./...
          go tool cover -func=coverage.out

      - name: Build all packages
        run: go build ./...

  deploy:
    name: Deploy API to production
    needs: ci
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    timeout-minutes: 20

    permissions:
      id-token: write
      contents: read

    steps:
      - name: Check out the repository
        uses: actions/checkout@v7

      - name: Setup Go
        uses: actions/setup-go@v7
        with:
          go-version-file: apps/api/go.mod
          cache-dependency-path: apps/api/go.sum

      - name: Build Linux release
        run: |
          mkdir -p dist
          CGO_ENABLED=0 GOOS=linux GOARCH=amd64 \
            go build -trimpath -ldflags="-s -w" \
            -o dist/ylx-api ./cmd/api

      - name: Create release checksum
        run: |
          cd dist
          sha256sum ylx-api > ylx-api.sha256

      - name: Configure temporary AWS credentials
        uses: aws-actions/configure-aws-credentials@v6
        with:
          role-to-assume: arn:aws:iam::541872890107:role/ylx-github-deploy-role
          aws-region: ap-south-1
          role-session-name: ylx-${{ github.run_id }}
          mask-aws-account-id: true

      - name: Upload release to S3
        run: |
          aws s3 cp dist/ \
            "s3://ylx-deployments-ap-south-1/releases/${GITHUB_SHA}/" \
            --recursive \
            --sse AES256

      - name: Deploy release through SSM
        env:
          INSTANCE_ID: i-0cb83dca50a8754d0
        run: |
          PARAMETERS=$(jq -nc --arg sha "$GITHUB_SHA" '{
            commands: [
              "set -euo pipefail",
              ("release_dir=/tmp/ylx-" + $sha),
              "install -d -m 0755 \"$release_dir\"",
              ("aws s3 cp \"s3://ylx-deployments-ap-south-1/releases/" + $sha + "/ylx-api\" \"$release_dir/ylx-api\""),
              ("aws s3 cp \"s3://ylx-deployments-ap-south-1/releases/" + $sha + "/ylx-api.sha256\" \"$release_dir/ylx-api.sha256\""),
              "cd \"$release_dir\"",
              "sha256sum -c ylx-api.sha256",
              "cp -a /opt/ylx/bin/ylx-api /opt/ylx/bin/ylx-api.previous",
              "install -o root -g root -m 0755 ylx-api /opt/ylx/bin/ylx-api.new",
              "mv -f /opt/ylx/bin/ylx-api.new /opt/ylx/bin/ylx-api",
              "if systemctl restart ylx-api && timeout 35 bash -c \"until curl --fail --silent --show-error --retry-connrefused http://127.0.0.1:8080/healthz; do sleep 1; done\"; then rm -f \"$release_dir/ylx-api\" \"$release_dir/ylx-api.sha256\"; rmdir \"$release_dir\"; else journalctl -u ylx-api -n 50 --no-pager || true; mv -f /opt/ylx/bin/ylx-api.previous /opt/ylx/bin/ylx-api; systemctl restart ylx-api; exit 1; fi"
            ]
          }')

          COMMAND_ID=$(aws ssm send-command \
            --instance-ids "$INSTANCE_ID" \
            --document-name AWS-RunShellScript \
            --comment "Deploy YLX API ${GITHUB_SHA}" \
            --parameters "$PARAMETERS" \
            --query "Command.CommandId" \
            --output text)

          echo "SSM command ID: $COMMAND_ID"
          sleep 2

          if ! aws ssm wait command-executed \
            --command-id "$COMMAND_ID" \
            --instance-id "$INSTANCE_ID"; then
            aws ssm get-command-invocation \
              --command-id "$COMMAND_ID" \
              --instance-id "$INSTANCE_ID" \
              --query '{Status:Status,Output:StandardOutputContent,Error:StandardErrorContent}' \
              --output json
            exit 1
          fi

          aws ssm get-command-invocation \
            --command-id "$COMMAND_ID" \
            --instance-id "$INSTANCE_ID" \
            --query '{Status:Status,Output:StandardOutputContent,Error:StandardErrorContent}' \
            --output json

      - name: Verify public health endpoint
        working-directory: .
        run: |
          curl --retry 5 \
            --retry-delay 2 \
            --fail \
            --silent \
            --show-error \
            https://ylx-api.akgbytes.in/healthz
