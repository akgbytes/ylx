{
  "name": "ylx",
  "private": true,
  "type": "module",
  "author": "akgbytes",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "build": "turbo build",
    "clean": "turbo clean && rimraf .turbo",
    "commitlint": "commitlint --edit",
    "dev": "turbo dev",
    "format": "prettier --ignore-unknown --write . && turbo format",
    "lint": "turbo lint",
    "run": "turbo run",
    "typecheck": "turbo typecheck",
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^21.2.1",
    "@commitlint/config-conventional": "^21.2.0",
    "husky": "^9.1.7",
    "prettier": "^3.7.4",
    "rimraf": "^6.1.2",
    "turbo": "^2.10.8",
    "typescript": "5.9.2"
  },
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": "10.0.0"
    }
  }
}
